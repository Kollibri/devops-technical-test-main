apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "conduit-deploy.fullname" . }}-test-connection"
  labels:
    {{- include "conduit-deploy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "conduit-deploy.fullname" . }}:{{ .Values.api.service.port }}']
  restartPolicy: Never
