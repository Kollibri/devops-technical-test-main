apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devopstest-webapi-chart.fullname" . }}-test-connection"
  labels:
    {{- include "devopstest-webapi-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devopstest-webapi-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
