apiVersion: v1
kind: Service
metadata:
  name: {{ include "devopstest-webapi-chart.fullname" . }}
  labels:
    {{- include "devopstest-webapi-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devopstest-webapi-chart.selectorLabels" . | nindent 4 }}
